# Engineering Review Notebook

`SQLite_Warehouse_Review.ipynb` adalah review console untuk menginspeksi input, schema, rejects, dan metric.

Aturan:
- jalankan dari root repository;
- restart kernel sebelum final run;
- implementasi tetap berada di source files;
- jangan hard-code acceptance numbers untuk membuat notebook terlihat lolos;
- final evidence harus sama dengan `run_pipeline.py` dan `validate_delivery.py`.
